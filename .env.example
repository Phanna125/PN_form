# Local development PostgreSQL (docker-compose service).
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/pn_form?schema=public"

# Optional: override persistence driver.
# prisma (default outside tests) | memory
PN_FORM_PERSISTENCE="prisma"

